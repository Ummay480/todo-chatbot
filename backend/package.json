{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend for petrol pump ledger automation",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest src/",
    "lint": "flake8 src/ && black --check src/",
    "format": "black src/"
  },
  "dependencies": {
    "better-auth": "^0.2.1"
  },
  "devDependencies": {}
}